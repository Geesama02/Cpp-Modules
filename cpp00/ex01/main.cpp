#include <iostream>
#include "PhoneBook.hpp"

int main()
{
	std::string cmd;
	PhoneBook my_phonebook;
	while (cmd != "EXIT")
	{
		std::cout << "Enter Command (ADD, SEARCH, EXIT): ";
		std::cin >> cmd;
		// if (cmd == "SEARCH")

	}
	return (0);
}
